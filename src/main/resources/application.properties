# SECURITY NOTICE: The database password previously hardcoded here (TTGG6676) was committed
# to version control and must be treated as compromised. Rotate/change this credential on the
# database server immediately, and purge it from the git history (e.g. via `git filter-repo` or
# BFG Repo-Cleaner) followed by a forced push and re-clone by all collaborators.
#
# Required environment variables (set these on the server, never commit real values):
#   DB_URL       - e.g. jdbc:mysql://localhost:3306/fullstack
#   DB_USERNAME  - e.g. root
#   DB_PASSWORD  - the rotated database password
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
